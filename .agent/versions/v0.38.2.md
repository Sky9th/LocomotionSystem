# v0.38.2 — 被动技能资产层

## 改动摘要

新增 5 个代表性被动技能（OnEquip/OnHit/OnKill/OnLowHP/OnEnterArea）的完整资产链：
JSON 定义 → Effect/Buff/Tag .asset → AbilityTree 集成。

修复两个 Editor Import 工具：
- AbilityImportExport: Passive 路径从 `Passives/` 修正为 `Definition/Passives/`
- AbilityTreeImportExport: 已有树改为更新而非 skip

新增 `PassiveBarOverlay` 枚举预埋（UI 脚本待后续）。
