# v0.10.6 — Property System: Description + 全量属性设计 + Editor UX

**日期**: 2026-06-10
**分支**: feature/ability-pipeline

## 改动

### PropertyDefSO: 新增 Description 字段
- `PropertyDefSO.cs`: 新增 `[TextArea] public string Description` 字段
- `PropertyDefSOEditor.cs`: Inspector 中显示 Description
- `PropertyImportExport.cs`: JSON DTO 加 `description`，导入/导出同步

### 全量属性体系设计
- `property-inventory.md`: 完整属性树设计文档（~185 Defs, 30 Trees, 8 族）
- `properties_export.json`: 完整 JSON 数据，所有 Def 带中文 description
- 经双 Agent 交叉验证修复（合并语义、路径兼容、缩放冲突、缺失属性等）

### Editor UX 改进
- `PropertyTreeEditorPopups.cs`: 
  - CreateDefDialog 加 Description 输入框
  - 新增 DefDetailPopup 只读详情弹窗（卡片布局: Header/Description/Constraints）
- `PropertyTreeEditorWindow.cs`:
  - 右侧属性池: 去掉 Type 列，悬停 tooltip 显示 `(Type) description`
  - 中心叶子卡片: 新增 `?` 详情按钮，悬停 tooltip 显示 Description + Type
  - 删除 Test/aaa 测试树

### 数据清理
- 删除 `Test.asset`, `aaa.asset` 测试树
- 删除 `test_import.json` 旧测试数据
- AmmoBase 扩展至 9 属性
- ArmorBase 扩展至 9 属性
- MeleeWeapon/Bow/Throwable/Shotgun 补齐 Presentation/Behavior
