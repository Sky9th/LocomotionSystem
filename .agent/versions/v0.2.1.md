# v0.2.1 (2026-05-29)

L1-L5 架构合规修复 + namespace 去重。

## 系统

- EventDispatcherService 从 L2 移至 L1_Core
- 6 个 L2 namespace 去重/去冲突：GameState, Player, GameScene, GameTime, GameCamera, GameInput
- UIService 解耦：TryResolveService 改为事件驱动（SGameStateRequest + SPlayerSpawnedEvent）
- PlayerService 清理：删除未使用的 CharacterActor 引用和 TryGetPlayerStats
- GameService 编辑器路径改为发布 SLoadSceneRequest 事件
- 清理 13 处死 using import
- 13 个 CreateAssetMenu 路径 Game/ → RedDust/
- 层级豁免规则文档化（契约 enum + event struct）
- .agent/README.md 目录树重写为 L1-L5 结构
- code-naming.md 从 archive 迁移至活跃 conventions
