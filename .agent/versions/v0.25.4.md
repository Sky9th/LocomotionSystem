# v0.25.4 — PropertyAgent 删除 + AbilityForest 搬家

- **删除 PropertyAgent.cs**：MonoBehaviour 壳移除，CharacterActor 直接持 PropertyTable（纯 C#）
- **删除 IPropertyReader.cs**：PropertyTable 直接暴露，读写权限由代码结构控制
- **AbilityForest 搬家**：L3_Character/Ability/ → L3_Ability/，namespace RedDust.Ability
- **CharacterActor**：新增 [SerializeField] propertyPreset，Properties 公开为 PropertyTable，Tick 由 Update 末尾驱动
- **全链改名**：Combat/Container/Physique/UIService/VitalsOverlay 全部 Consumer 更新
- **AbilityExecutor**：Buff 注入注释 + TODO（后续整体重构）

## 设计决策
- PropertyAgent 壳被删除：Inspector 拖 _preset 移到 CharacterActor，GetComponent 发现改用临时方案，Tick 由 Actor 驱动
- IPropertyHost 未创建：YAGNI——当前仅 CharacterActor 有属性，后续多实体时再抽接口
- 写权限暴露为已知风险：公开 PropertyTable（非接口），由代码审查 + 架构债 TODO 约束

## 状态
纯重构，零功能变更。Buff 注入回归已知（当前游戏中无 CharacterActor 外实体）。
