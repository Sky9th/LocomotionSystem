# v0.22.4 — AbilityReactor 生命周期修复 + GripTag 解耦

- **Type**: bugfix
- **Scope**: ability, character
- **Changes**:
  - AbilityReactor: MonoBehaviour → ModuleChildMono，hitEvent 解析从 Awake 移至 OnWire，消除与 EventHub 的 Awake 竞争窗口
  - CharacterActor: 添加 `[RequireComponent(typeof(AbilityReactor))]` 防御
  - CharacterBuildContext: 新增 `OwnedGripTags` 独立 grip tag 存储，与 AbilityExecutor 解耦
  - CharacterActor.Update / PlayerDirector: grip tag 读写从 `Ability?.OwnedTags` 迁移至 `OwnedGripTags`
  - NPC.prefab / PathFinding.unity: 场景中 NPC 激活 + 预制体清理
