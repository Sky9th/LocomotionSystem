# v0.38.7 — 站姿/蹲姿切换 + 动画资产补全

## 背景
S5 动画系统补完进入实施阶段。Head Look IK 和 Footstep 对俯视角游戏优先级低，延后并清理旧代码。52 个缺失动画 FBX 从 External PROTOFACTOR 集导入。实现站立↔蹲姿切换（跳过趴姿）。

## 改动

### 动画资产补全
- 从 External 导入 24 个 FBX（2H_Fist x11, 1H_Blade Relax x9, 1H_Sidearm Relax x4）
- 覆盖 Crouch/Air/Land/Turn 缺口
- 2H_Fist 目录重构为 Combat/ 子目录结构（与 1H_Blade/1H_Sidearm 一致）
- animation_all.json 更新所有 GUID 引用

### Head Look + Footstep 延后
- 删除 CharacterHeadLook.cs（headLookMixer 从未赋值，运行时死路径）
- AnimationBrain: TotalLayerCount 7→6, 移除 HeadLook 层/mixer/UpdateHeadLook/FreezeHeadLookChildren
- CharacterKinematic: Evaluate() → Vector2.zero
- LocomotionAnimationConfigSO + AnimationImportExport: 移除 headLookSmoothingSpeed
- AnimationBrain + CharacterAudio: 注释 Footstep 桥接线
- 所有移除处保留 IK/Footstep 占位注释

### 站姿/蹲姿切换
- LocomotionAnimationSetSO: 新增 crouchIdle + crouchMixer 字段
- GroundLocomotion: Crouching → Crawl gait（移动时）/ Idle gait（静止时）
- BaseIdleState: Posture==Crouching 时用 crouchIdle，fallback idleL
- BaseMovingState: Crawl → crouchMixer + 蹲姿 fallback 防站姿闪烁
- AnimationImportExport: DTO + Export/Import 增加 crouch 字段 + crawlAnimNativeSpeed
- 按键: C=蹲下, X=站起（InputAction 已有绑定）

## 修复
- 蹲姿到达目标点后短暂闪现站立动画：BaseMovingState fallback 按 posture 选择 crouchMixer/walkMixer
