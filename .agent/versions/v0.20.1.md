# v0.20.1

## 概览

`.claude` 文档工作流重构 — rd-doc 拆分为编排器 + 3 独立子技能，rd-commit 委托给 rd-doc。

## 变更

### 文档工作流重构
- `rd-doc` 重写为编排器 — 判断归档类型 → 调度子技能 → 质量检查 → 输出报告
- `rd-session-doc` 新增独立 skill — 会话工作日志模板与规则
- `rd-design-doc` 新增独立 skill — 设计文档模板与规则
- `rd-tech-doc` 大幅重写 — 技能独立化，模板与规则重组
- `rd-commit` 简化 — 文档归档步骤委托给 rd-doc
