# v0.27.0 — 嵌套容器 + L4 Equipment 子模块

## 改动

- Entity +NestedContainer 属性，EntityService.Register 自动读 Slots/ 子节点创建嵌套 Container
- EntityService.Unregister 级联清理子实体
- Container.Tick(dt, depth) 递归传播 + 环检测 (maxDepth=10)
- ContainerSlot PropertyDefSO (Capacity=20) + Backpack PropertyTree (Slots/ContainerSlot)
- L4 CharacterEquipment 子模块：每帧 diff Container 状态 → 管理武器 GO 生命周期 + 同步 OwnedGripTags
- SlotBoneMapper (SlotId→HumanBodyBones 静态映射)
- PlayerDirector 移除 ResolveBodyForm (BodyForm 恒 Relax)
- PlayerService 测试代码清理：武器进背包，删本地 Container 创建
- Blade/Pistol 资产加 Equip.Grip.* 标签
