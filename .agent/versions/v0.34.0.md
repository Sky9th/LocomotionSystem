# v0.34.0 — RdTag 重命名 + AnimationClip 直引 + Ability 数据管线就位

## 架构

### rTag → RdTag 全量重命名
- 目录 `L1_Core/rTag/` → `L1_Core/RdTag/`
- 类 `rTag` → `RdTag`, `rTagDefSO` → `RdTagDefSO`, `rTagContainer` → `RdTagContainer`
- `rTagImporter` → `RdTagImporter`, `rTagImportWindow` → `RdTagImportWindow`
- `FindAssets("t:rTagDefSO")` → `FindAssets("t:RdTagDefSO")`（10 处）
- CreateAssetMenu `"RedDust/rTag"` → `"RedDust/RdTag"`
- 17 文件 40+ 处注释/字符串同步修正

### StringAsset → AnimationClip 直引
- `AbilityActivationSO.animationAsset` (StringAsset) → `animationClip` (AnimationClip)
- `AnimationRequest` 移除 `Alias`/`HasAlias`
- `AbilityDriver` 移除 TryPlay alias 分支
- `ActivationImportExport`: `ClipToJson`/`ClipFromJson` 用 `{FBX_GUID}|{ClipName}` 格式
- 删除 `animation-alias-profile.md`（类已不存在）

## Ability 数据管线

### Tags（+18 新标签）
- Cost 域: Cost + Stamina/HP/Ammo/Durability/Fuel/Charge (7)
- Impact 子标签: Light/Medium/Heavy/Pull (4)
- Damage: Explosive (1)
- Blade 技能: SlashB/Thrust/Combo2A/Combo3A/Combo3B (5)
- Pistol2H: SuppressiveFire (1)

### Activations（+6 新激活）
- AttackB, AttackD_Thrust, 2HitComboA, 3HitComboA, 3HitComboB, DrawWeapon

### Abilities（+5 新技能）
- SlashB, Thrust, 2HitComboA, 3HitComboA, 3HitComboB

### Code Review 修复
- ActivationEditorWindow: ObjectField 同步 animationClip
- ActivationImportExport: `|` 分隔符改为固定 32 位 GUID + Debug.LogWarning
- BuildTagLookup 去重 → `RdTagLookup.Build()`
- ClipFromJson: `_fbxClipCache` 缓存
- WeaponAttachPoint: float 精度四舍五入
- GripAnimationTableSO: Debug.Log 加 `#if UNITY_EDITOR`
- Effect JSON: Impact/Cost effectTag 修正为 `Ability.*` 前缀
- 所有 ImportExport 窗口设默认 JSON 路径

## 技术文档
- 新增 `RdTagLookup` 工具类
- 删除 `animation-alias-profile.md`
- 更新 `gameplay-tag-ability.md`, `ability-activation-assets.md`
- 更新 `animation-request.md`, `traversal-driver.md`, `base-layer.md`
