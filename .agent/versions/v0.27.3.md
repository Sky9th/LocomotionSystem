# v0.27.3 — AbilityDefSO → ActiveAbilitySO 重构

**Date:** 2026-06-29

## Changes

- 删除 `AbilityDefSO`，替换为 `ActiveAbilitySO` + `PassiveAbilitySO`
- `AbilityExecutor` ConditionCallback / TryActivate 签名更新
- `CharacterActor` 新增装备变化 → AbilityForest.SetWeaponTags 同步
- 编辑器文件同步更新命名
- SO 资产 GUID 更新
