# v0.40.5

**S5.4 AirLand 分级落地** — BaseAirLandState 从 landLight/landHard 二选一改为 Gait 驱动 LinearMixer 混合参数（0=Idle, 1=Walk, 2=Run/Sprint）。

**S5.5 Traversal 动画迁移** — TraversalDriver.Evaluate() 和 ResolveClimbClip() 从旧 AnimationAliasProfile 路径迁移到 LocomotionAnimationSetSO traversal 字段。触发逻辑增加 Dot Product 方向验证（`dot(moveDir, -obsNormal) > 0.8`），跨引擎标准公式区分"正面顶墙"vs"擦墙而过"。

**PathfindingService 缓存修复** — OnAssemble() 不再无条件 Scan()，`cacheStartup && file_cachedStartup != null` 时跳过，尊重 A* GraphCache.bytes。
