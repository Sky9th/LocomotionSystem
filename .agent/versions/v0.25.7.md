# v0.25.7 — PlayerService 接入 EntityService

> 2026-06-28

## 改动

- `SEntitySpawnRequest` 字段调整：`EntityId` → `Preset`（请求方不分配 Id）
- `EntityService` 补事件订阅：SpawnRequest/DespawnRequest → Spawned/Despawned
- `EntityService` 移除公开 `Spawn()`/`Despawn()` 方法——全走事件通道
- `PlayerService` 改造：`playerPrefab` → `CharacterDefSO`，通过事件通道发 SpawnRequest
- `PlayerService` 不持有 EntityService 引用
- Entity 事件通道命名统一：`EntityXxxEvent`（文件 + 类名 + CreateAssetMenu）
