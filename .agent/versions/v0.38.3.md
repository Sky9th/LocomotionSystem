# v0.38.3 — 被动技能触发管线 + FloatAdjunct Max 扩展 + PassiveBarOverlay

## 改动摘要

被动技能从纯资产落地为完整运行时管线：
- InstanceManager 桥接（SyncInstances + OnEquip 触发）
- OnHit/OnKill/OnDamaged dispatch 点
- FloatAdjunct 新增 MaxAdd/MaxMultiply 支持扩容
- PassiveBarOverlay 脚本 + UI 注册
- Buff 效果应用链路修复（Resolve 提前 + TryGetPath def→路径）
- ExecutionState 收集技能 targetEffects 中的 DamageEffectSO
- BerserkerBlood 属性从 AttackSpeed/MoveSpeed 改为 Agility
