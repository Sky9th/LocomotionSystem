# v0.40.6

## Changes

### Addressables 兼容修复
- **Importer Addressables 集成**：9 个 Importer 创建新资产后自动标记 `boot` label，Build 不再遗漏
- `DataLabelTools.EnsureBootLabel(assetPath)` 新增，从 L2_SceneService 移至 Shared/Editor
- `TagImportExport` 反射修复：`RefreshCache` 改为 public 后搜索标记适配

### Editor UI 修复
- `EditorButton` overflow 清除 + 按 size 设 fixedHeight（Medium=24, Large=28）
- `ImportExport` 按钮 Large→Medium + Result errors 加 ScrollView

### 清理
- `Human.asset` 删除孤儿字段 `innateTreeIds`

### 计划
- `short-term.md` 新增 P5.0 基础设施补完（ItemEditor + ItemImportExport + 兼容验证）
