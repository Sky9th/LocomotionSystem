# v0.9.5 — Editor 菜单统一 + Tag 导入器 + AbilityEditor 修复

2026-06-07

## 改动

- 统一所有 Editor 菜单至 `RedDust/` 根（6 个窗口）
- 新增 `GameplayTagImporter` — 从 JSON 批量创建 GameplayTag 资产，支持模组
- 新增 `tags_closed_loop.json` — 闭环测试 Tag 导入文件
- 修复 `AbilityEditorMiddlePanel` 编译错误（删除 categoryTag 时遗留的 `var v`）
- 回滚误导入的 Ability/Noise/Stat Tag 目录及其 .meta
