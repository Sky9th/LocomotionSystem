# v0.45.3 — AssetCatalog contentId 地址系统

## 改动

- **AssetCatalog**: 名称索引 → contentId 索引（`_byContentId` 统一字典），FindItem/FindCharacter 两步查找（精确匹配 → 自动补 `rd.` 前缀），InitCharacters+InitItems 合并为 InitPresets
- **PropertyPresetSO**: 新增 `_contentId` 序列化字段，Editor Save / JSON Import 时自动写入，运行时零解析
- **ContentIdUtility**: 删除（`Common/Id` 已存完整路径，拼接逻辑冗余）
- **CommonConstants.OfficialNamespace**: `"rd."` 提取为全局常量
- **PlayerService**: 硬编码 asset name → contentId，`[SerializeField] string` → `const string`
- **59 实体 JSON**: 全量补齐 `Common/Id` 完整路径
- **plans/**: 6 个过期计划删除，2 个移入 design/tech
