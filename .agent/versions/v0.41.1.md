# v0.41.1 — Entity 数据审核 + Tag 对齐 + 测试数据补全

## 改动摘要

- **Tag 树补全** `tags_all.json`：+7 Tag（Building.Defense.Wall / Crafting.Station / Material.Wood / Attachment.Scope）
- **Building Tag 路径修正** `building_all.json`：Tag 路径加 `Entity.` 前缀，对齐 Equipment 命名规范
- **Equipment 修正** `equipment_all.json`：description 过期文本 + `Attachment.Scope` Tag 路径
- **空模块测试数据补全**：Ammo (+3)、Consumable (+4)、SceneItem (+2) 共 9 条实体
- **Unity 自动同步**：3 个 Building .asset OverridesJson 更新、NewBuildingDef.asset 移除（空模板）、Human.asset templateId 清理、Boot.asset 更新

## 数据审核记录

所有 6 个 `*_all.json` 完成审核：
- `entityType` 与 TypeMap 一致 ✓
- 所有 `templateName` 引用的 PropertyTreeSO 存在 ✓
- 所有 Tag 路径在 `tags_all.json` 中存在 ✓
- 全部 25 个实体条目覆盖所有 L3 模块 + 叶子模板
