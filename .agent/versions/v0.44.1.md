# v0.44.1 — Entity contentId 基础设施

## P0: Common/Id + Common/Category

- PropertyTree Entity 根新增 `Common/Id` [String] + `Common/Category` [RdTag]
- `ContentIdUtility` — contentId 拼接工具（`rd.{Category}.{Id}`）
- `PropertyTable.GetRdTag()` — RdTag 语义读取薄封装
- EntityEditorWindow — Basic 段 contentId 只读预览，首次保存自动推导 Id
- 49 件 P5.0 物品全量落数据（Category + Id）

## 弹药 Tag 迁移

- `Ammo.Caliber.*` → `Entity.Ammo.Caliber.*`
- Tag 树补 `Entity.Ammo.Caliber` + 4 口径子节点 + `Blunt`

## 性能优化

- EntityEditorWindow 模板缓存（5s TTL）— 消除每帧 AssetDatabase 全盘扫描
- 属性分组预计算 — 从 SelectPreset 迁移，不再每帧重建
- TagTreeModel.GetCached() — TagPicker 打开时不再重复扫描

## 涉及

63 files, +531/-200
