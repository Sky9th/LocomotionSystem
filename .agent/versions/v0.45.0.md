# v0.45.0 — HybridCLR 基础设施落地

架构层变更。HybridCLR 社区版接入完成，IL2CPP 获得 `Assembly.Load()` 能力。

### 变更
- **HybridCLR 接入**: 安装社区版包，配置 IL2CPP + .NET Framework
- **运行时代码**: AssetService.LoadAOTMetadata() + SceneService boot 流程集成
- **元数据管线**: 66 程序集补充元数据接入 Addressables (AOTMetadata Group, aot-metadata label)
- **构建管线**: HybridCLRData/ + hybridclr/ + il2cpp_plus/ 入 .gitignore
- **工程配置**: manifest.json + ProjectSettings + AddressableAssetSettings 更新

### 验证
- Editor 模式下 AOT metadata 66 assemblies 加载成功
- Generate → All 产物路径确认
