# v0.34.1 — UI Ability/Weapon 槽位 + 调试面板（纯展示）

## 新增

- **UIIconSlot**：通用槽位组件（icon + 冷却 Radial360 遮罩 + 选中边框 + 快捷键标签），`[ExecuteAlways]` + DOTween 动画
- **AbilityBarOverlay**：动态技能槽（`ResolvedActives.Length` 个槽位），轮询冷却/选中状态，输入代码已注释留待重构
- **WeaponBarOverlay**：动态武器槽（背包 `AllItems()` 个槽位），轮询物品列表
- **DebugOverlay**：管道状态 + 冷却计时 + 驻留时间显示

## 修改

- **AbilityExecutor**：新增 `Pipeline` getter + `GetCooldownRemaining()` + `GetAbilityCooldownRemaining()`（UI 查询 API，标注 TODO 待提取）
- **ActiveAbilityPipeline**：新增 `CurrentState` 属性（UI 查询，标注 TODO）
- **UIService**：新增 `PlayerActor` 属性（标注 TODO — BuildContext 后门），Playing 时自动显示 AbilityBar/WeaponBar Overlay
- **UIOverlayId**：新增 `AbilityBarOverlay`, `WeaponBarOverlay`, `DebugOverlay`

## 决策

- [ui-communication-architecture.md](../tech/L2-services/L2-ui/ui-communication-architecture.md)：UI ↔ L3 通信架构决策记录（Query/Event/Input/Command 边界）
- BuildContext 外部引用全部标注 TODO，指向统一引用获取路径（GameContext + EntityService + PlayerID）

## 删除

- **ActionBarOverlay.cs**：武器/技能合并方案废弃
- **PlayerDirector.ProcessSkillInput**：技能激活已迁至 AbilityBarOverlay（后续将迁至 PlayerSkillHandler）
