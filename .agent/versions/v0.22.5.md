# v0.22.5

## 摘要

TraversalAnimationSetSO 并入 LocomotionAnimationSetSO，动画数据目录引入 Style 子目录，Zombie 动画配置落地。

## 改动

- **SO 合并**: `TraversalAnimationSetSO` 删除，6 个 traversal 字段并入 `LocomotionAnimationSetSO`
- **导入导出**: `AnimationImportExport` 移除 TraversalSet 独立 phase，补全 `GripEntryItem.combatSet` 支持，`LocomotionSetEntry` 新增 traversal 字段
- **数据目录重构**: Human 数据移入 `Human/Male/`，新增 `Zombie/TypeA|B|E/` 风格子目录
- **Zombie 动画**: 3 套 Zombie 动画配置 JSON 落地，PROTOFACTOR Zombie FBX 文件导入
- **PROTOFACTOR 重构**: Human FBX 文件从根层级移入 `Human/` 子目录
