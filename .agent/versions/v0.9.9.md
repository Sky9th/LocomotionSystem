# v0.9.9 — 精简冗余 Stat + rd-commit 交互优化

2026-06-08

## 改动

- 移除 `MaxHP` / `MaxStamina` 冗余定义（Stat 自身 Min/Max 已定义范围）
- Actor Tree 精简为 1 stat (HP)，Human 从 57→56 defRefs
- Stamina Max 拓宽至 200 覆盖原 MaxStamina 范围
- rd-commit skill 新增 `AskUserQuestion` 按钮确认交互
- 全量资产重导入验证通过（134 defs + 21 trees）

## 设计决策

- StatDefinitionSO.Min/Max 已足够约束 Current 范围，不需要独立 MaxXxx 定义
- 运行时上限调整通过 StatModifier 作用于 StatInstance.Current 而非新建 Stat
