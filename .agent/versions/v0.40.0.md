# v0.40.0 — 加载架构重组

## 加载管道

- `Pipeline/Boot/` — BootTask 按领域拆分（Property/Tag/Ability/Item/Character），BootTaskComposer 统一编排
- `Pipeline/Scene/` — PrototypeArtTask 示例，Scene 层预留
- `Pipeline/Streaming/` — 预留
- 删除 LoadingOrchestrator（被 BootPipeline 替代）

## RdTagDefSO

- 去掉懒加载，恢复简单 RefreshCache()
- TagBootTask 在 boot 阶段 BFS 重建 FullTag 缓存，解决 Build 下 OnEnable 顺序问题

## SceneAssetLabel

- 精简为 Boot + PrototypeArt，移除未使用的 14 个 label

## 目录

- `AddressableAssetsData` 移至 `Assets/Settings/`
- `Background/`、`Streaming/` 空目录清理
