# v0.12.1

## Revert: 回滚删除按钮组件化改动

回滚部分 Editor UI 组件化 commit (70092a73) 中与删除按钮相关的改动，恢复手动红底 `GUI.backgroundColor` + `EditorStyles.miniButton` 模式。

### 回滚的文件
- `UIUtility.cs` — 恢复 `DeleteButton()` 为旧版（不带 `EditorButtonSize.Small`），搜索清除按钮恢复 `EditorStyles.miniButton`
- `PropertyTreeListView.cs` — 删除按钮恢复手动红底
- `PropertyTreeEditorWindow.cs` — 3 处文件夹/属性/def池删除按钮恢复手动红底，恢复 `ColorDelete` 字段
- `AbilityEditorWindow.cs` — 移除树节点 `onDeleteLeaf` 回调
- `AbilityEditorMiddlePanel.cs` — 恢复 `✕` 清空按钮的 `EditorButtonStyle.Danger`

### 保留不改
- `PropertyImportExport.cs` — EditorImportExport 组件化重写
- `PropertyTreeEditorPopups.cs` — Create/Cancel/Close 按钮改用 EditorButton
- 所有 `*ImportExport.cs` — 导入导出窗口重构
- `PropertyTreeEditorWindow.cs` — 搜索行/Add Folder/详情按钮的组件化
- `EffectEditorWindow.cs` / `NoiseEditorWindow.cs` — Tag 按钮改用 EditorButton
