# v0.40.7

## ItemEditor 窗口

- **新增** `ItemEditorWindow` — 物品数据编辑器（L3_Item/Editor/）
- 左中右三栏布局：Item Tree (EditorTreeView) | Editor (Basic/Slots/Properties) | Preview (Icon + Prefab)
- 属性表单按 PropertyTree 一级节点分组为独立 EditorCard
- Struct 属性按类型分发表单（SlotDef 专用编辑器 + 未知类型只读提示）
- Tag 编辑抽取为共享 `DrawTagChips` 组件（横向 chip + × 删除 + + 添加）
- 修复：`ResolveStructureEditor` 绕过 GameService.Instance，用 AssetDatabase 直接查找 PropertyDefSO
- 修复：叶子节点孤儿属性打 warning 并跳过
- 设计文档更新为当前实现状态

## 其他
- PropertyTable: 清理未使用代码
- PropertyTreeEditorWindow: 微调
