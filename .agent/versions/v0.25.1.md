# v0.25.1 — Entity 基树 + Editor 修复

## Entity 基树

- 新增 `Entity` 隐式基树——所有 9 棵根树共享 4 个属性
- `Common/DisplayName` (String) / `Icon` (AssetRef) / `Weight` (Float) / `Slots` (Struct, 默认 [])
- `properties_all.json`：Entity 树定义 + 9 根树 `inheritsFrom: "Entity"` + 删除 20 个重复节点
- 纯数据改动，`PropertyTreeSO.cs` 零代码变更——import 时自动建立继承链

## Editor 修复

- 左面板：过滤 Entity（按名隐藏隐式基树）
- 中心面板：根级叶节点渲染（Environment 的 FogDensity 等不再空白）
- Entity 的 Common 文件夹及其属性以灰色"继承"样式显示

## 状态

代码和数据完成，需在 Unity 中 Import JSON 重建 Tree .asset 文件。
