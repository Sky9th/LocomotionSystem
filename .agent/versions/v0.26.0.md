# v0.26.0 — Slots PropertyTree 改造 + Entity→CharacterActor 数据管线

## Slots 架构重构
- `Common/Slots` (Struct 数组) → `Slots/` 文件夹 + 独立 PropertyDefSO ×14（每个槽位专属默认值）
- Entity 基树: `Slots/` 空文件夹
- Human 树: +9 身体槽位 (RightHand→Back, defId 各专属)
- BodyArmor 树: +3 Pouch 槽位
- Firearm 树: +3 附件槽位 (Scope/Magazine/Muzzle)

## 近战武器属性树
- Blade / Axe / Blunt / Spear 四子类型 ← MeleeWeapon
- +BleedChance (Float) / +ArmorPierce (Float)

## Entity→CharacterActor 管线
- EntityService push Properties → Identity → CharacterActor 单向数据流
- CharacterActor 删 propertyPreset / Properties 属性
- CharacterBuildContext 持 Identity 引用，Properties 改为转发属性
- PropertyTable +GetChildren 文件夹遍历

## Container
- SlotDef: AcceptTags GameplayTagDefinitionSO[] → string[]
- CharacterContainer.OnWire: GetChildren("Slots") + GetStruct
- CharacterDefSO 清理 ISerializationCallbackReceiver hack

## 动画兜底
- BaseAirLoop / BaseAirLand 空动画时跳过播放，直接跳 Idle

## 工程
- CreateAssetMenu 归拢: CharacterDefSO / ItemDefSO → RedDust/Entity/
- PlayerService 测试 spawn: Zombie + Blade (RightHand)
