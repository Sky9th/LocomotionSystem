# v0.25.0 — PropertyType.Struct + 模块重命名 + Editor 修复

## 模块重命名

- `EntityDefSO` → `PropertyPresetSO`（属性预设 — Template + OverridesJson）
- `EntityProperties` → `PropertyTable`（运行时属性平表）
- 静态工厂 `Create(def)` → `FromPreset(preset)`
- `PropertyAgent._props` → `_table`，`_def` → `_preset`
- 全模块 Property 前缀统一：Def / Tree / Preset / Table / Agent

## PropertyType.Struct

- `PropertyType.Struct` 枚举新增（末尾，不破坏序列化）
- `PropertyDefSO` + `StructTypeName` + `DefaultStructJson` 字段
- `PropertyTable` + `_structJsons` 字典 + `DoWrite` Struct case（三路径统一 + 自动包裹）
- `GetStruct<T>(path)` / `GetStructArray<T>(path)` — Type.GetType() 运行时校验
- `PropertyAgent` 透传 `GetStruct<T>` / `GetStructArray<T>`

## [PropertyStruct] 属性化系统

- `PropertyStructAttribute` 标记 struct
- `PropertyStructScanner` — Editor 扫描 + 下拉框，自动填 FullName
- `Type.GetType()` 解析 → `typeof(T)` 校验，不匹配报错
- `SlotDef` 标记 `[PropertyStruct]`

## Editor

- `PropertyDefSOEditor` Struct 下拉框
- `PropertyTreeEditorPopups` / `PropertyImportExport` 全链路 Struct case
- **Bugfix**: 文件夹重命名 — IMGUI 失焦复位导致改名无效。改用 `_folderEdits` 缓存编辑态

## 数据

- `properties_all.json` Actor 树 + `Slots` 属性（Struct/SlotDef/[]）

## 状态

代码编写完成，待 Unity 编译验证。
