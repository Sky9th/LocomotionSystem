# v0.25.2 — L3_Container 模块 + 角色身体槽位

- **新建 L3_Container 模块**：SlotDef / ContainerSlot&lt;T&gt; / Container&lt;T&gt; / ContainerSlotRef
- **SlotDef 从 L3_Item → L3_Container**：Container 为唯一运行时消费者，PropertyTree 仅存储层
- **ItemDefSO 零 C# 字段落地**：移除 SlotDef/Slots/OnValidate，槽位走 PropertyTree Struct
- **CharacterContainer**：角色身体槽位适配层，OnWire 从 Common/Slots 读取
- **CharacterDefSO 默认注入**：StandardBodySlots → OnBeforeSerialize 自动填充 OverridesJson
- **CharacterActor 持有 Container**：`new CharacterContainer(buildCtx, Registry)` via ModuleChild 生命周期

## 设计决策
- BodySlots 从 PropertyTree 读取，无代码 fallback（非人形角色不该有标准 9 槽位）
- ModuleChild.OnWire 读依赖组件——避免 Unity Awake 顺序竞态

## 状态
Container API 就位，等待 ItemInstance 到位后可实际 Place/Remove 物品。
