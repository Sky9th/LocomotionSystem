# v0.25.3 — Code Review 整理：命名 / 访问修饰符 / 生命周期 / 错误处理

- **agent → propertyAgent 全链改名**：CharacterActor、BuildContext、CharacterContainer、CharacterCombat
- **访问修饰符降级**：Physique、Container internal → private（无外部消费者）
- **PropertyAgent 执行顺序**：+DefaultExecutionOrder(-11) 保证 Awake 先于 CharacterActor
- **PropertyTable 读方法**：路径不存在 → Debug.LogError 替代静默返回 0f/null
- **架构债标记**：Properties 公开暴露（L2→L3）、饥饿消耗硬编码（Actor 内联业务逻辑）

## 状态
纯重构与 bug 修正，无功能变更。
