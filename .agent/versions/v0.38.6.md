# v0.38.6 — Head Look + Footstep 延后，旧代码清理

## 背景
Head Look IK 和 Footstep 事件桥接对俯视角游戏优先级非常低。旧 Vector2MixerState 方案从未生效（headLookMixer 始终为 null）。本次清理旧代码并注释桥接线，保留占位。

## 改动

### Head Look 全量清理
- 删除 `CharacterHeadLook.cs` + `.meta`（静态工具类，49行）
- `AnimationBrain.cs` — TotalLayerCount 7→6，移除 HeadLook=5 层/字段/mixer/UpdateHeadLook()/FreezeHeadLookChildren()，Footstep 重编号为 5
- `CharacterKinematic.cs` — `CharacterHeadLook.Evaluate()` → `Vector2.zero` + 注释
- `LocomotionAnimationConfigSO.cs` — 移除 `headLookSmoothingSpeed` 字段
- `AnimationImportExport.cs` — 移除 DTO + Export/Import 中的 `headLookSmoothingSpeed`

### Footstep 事件桥接延后
- `AnimationBrain.cs` — 注释 `FootstepCallback` 桥接线，保留 `OnFootstep` 事件占位
- `CharacterAudio.cs` — 注释订阅/取消订阅，保留 `HandleFootstep()` 占位

### 计划更新
- S5.1 Footstep → 🔒 延后
- S5.2 Head Look IK → 🔒 延后
- S5 耗时估算 ~2.5天 → ~1天
