# v0.9.6 — Tag JSON 导入导出系统 + 全量 Tag 树落地

2026-06-07

## 改动

- 新增 `TagImportExport.cs` — JSON ↔ .asset 批量导入导出，支持模组
- 新增 `tags_all.json` — 199 个 GameplayTag 全量定义 (Actor + Ability + Noise)
- 废弃旧 GameplayTagImporter.cs，功能并入 TagImportExport
- 统一 Editor 菜单至 RedDust/ 根，priority 分组含分割线
- 修复 `CreateAsset` 后 name 未赋导致 leafName 为空的 Bug
- 修复 AbilityEditorMiddlePanel 编译错误 (删除 categoryTag 遗留 var v)

## 设计决策

- Tag 目录由 parent FullTag 自动派生（点→斜杠），无需手工指定
- 导出走 parent 链计算 FullTag，不再依赖非序列化 cachedFullTag
- Stat 标签移除 — StatDefinitionSO 直接引用，不经过 GameplayTag
